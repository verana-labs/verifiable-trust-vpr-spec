node_modules
.DS_Store

# generated by the spec-up render (rebuilt in CI on every deploy)
# note: index-v3.html is the static v3 stable spec and stays tracked
/index.html
/assets/
/build/
/package-lock.json
